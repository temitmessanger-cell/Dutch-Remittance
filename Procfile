# Dutch Remit — Railway deployment
# ----------------------------------
# Railway runs this file as the start command.
# The build command in railway.json builds Flutter web first.
web: cd Backend && node server.js
